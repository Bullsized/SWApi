import { GetImdbLinkPipe } from './get-imdb-link.pipe';

describe('GetImdbLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new GetImdbLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
