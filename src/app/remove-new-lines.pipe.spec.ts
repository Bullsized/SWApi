import { RemoveNewLinesPipe } from './remove-new-lines.pipe';

describe('RemoveNewLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNewLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
